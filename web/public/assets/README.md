# Web Assets

Put static files here (logos, images, icons) and reference them by absolute path.

Examples:

- `/assets/logo.svg`
- `/assets/hero.jpg`

This folder is served as-is by Vite (from `public/`).
