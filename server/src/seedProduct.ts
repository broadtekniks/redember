export async function ensureSeedProduct(): Promise<void> {
  // Hard-coded products removed - use Admin UI to create products
  console.log("Product seeding skipped - manage products via Admin UI");
}
